# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local secrets (never commit)
.env
.env.local

# Playwright
/playwright-report/
/test-results/
/blob-report/

# Local MCP server config (personal tooling)
.mcp.json

# Playwright MCP session artifacts
.playwright-mcp/

# Client source materials — must never deploy. public/ ships verbatim to the
# live site; the xlsx holds real customer PII and the raw photo folders are
# multi-MB originals (optimized copies live in public/images/dishes/).
public/*.xlsx
public/Files/
public/images/Updated Menu Photos/
public/images/Catering/
public/images/Menu/
